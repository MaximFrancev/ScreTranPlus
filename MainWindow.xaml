<Window
    x:Class="ScreTranPlus.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:s="clr-namespace:System;assembly=mscorlib"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ScreTranPlus"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    d:DataContext="{d:DesignInstance Type=local:MainWindowModel}"
    Width="600"
    SizeToContent="Height"
    mc:Ignorable="d"
    Title="ScreTran+" Background="#FF313338" WindowStyle="None" ResizeMode="NoResize" Loaded="Window_Loaded"
    UseLayoutRounding="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="34"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
            <local:BoolToObjectConverter x:Key="BoolToInvertedBoolConverter">
                <local:BoolToObjectConverter.TrueValue>
                    <s:Boolean>False</s:Boolean>
                </local:BoolToObjectConverter.TrueValue>
                <local:BoolToObjectConverter.FalseValue>
                    <s:Boolean>True</s:Boolean>
                </local:BoolToObjectConverter.FalseValue>
            </local:BoolToObjectConverter>
            <Style TargetType="Label">
                <Setter Property="Margin" Value="0,6,0,4"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>
            <Style x:Key="StandardButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="200"/>
                <Setter Property="Height" Value="35"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="{x:Null}"/>
                <Setter Property="Background" Value="#FF4E5058"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#FF6A6C77"/>
                    </Trigger>
                </Style.Triggers>
                <Style.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5" />
                    </Style>
                </Style.Resources>
            </Style>
            <Style x:Key="CaptionButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="44"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="LightGray"/>
                <Setter Property="Background" Value="{x:Null}"/>
                <Setter Property="BorderBrush" Value="{x:Null}"/>
                <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#FF2C2E32"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding CloseCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Border x:Name="MainWindowBorder">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid x:Name="AppTitleBar" Background="#FF1E1F22">
                <TextBlock
				    Text="ScreTran+"
				    Margin="15,0,0,0"
				    FontFamily="Segoe UI Black"
				    FontSize="14" 
                    Foreground="Gray"
                    VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="MinimizeButton" Style="{StaticResource CaptionButtonStyle}"
						Content="&#xE949;"
						Click="MinimizeWindow"/>
                    <Button x:Name="MaximizeButton" Style="{StaticResource CaptionButtonStyle}"
						Content="&#xE739;"
						Click="MaximizeWindow"/>
                    <Button x:Name="RestoreButton" Style="{StaticResource CaptionButtonStyle}"
				        Content="&#xE923;"
				        Click="RestoreWindow"/>
                    <Button Style="{StaticResource CaptionButtonStyle}"
						Content="&#xE106;"
						Click="CloseWindow"/>
                </StackPanel>
            </Grid>
            <Grid x:Name="HotkeyDialog" Grid.Row="1" Panel.ZIndex="20" d:Visibility="Hidden" Visibility="{Binding IsKeySetting, Converter={StaticResource BoolToVisConverter}}">
                <Grid.Background>
                    <SolidColorBrush Color="Black" Opacity="0.5"/>
                </Grid.Background>
                <Border CornerRadius="5" VerticalAlignment="Center" HorizontalAlignment="Center" Background="#FF232428" Padding="50">
                    <StackPanel >
                        <TextBlock Text="Press any key..." FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="White"/>
                        <TextBlock/>
                        <TextBlock Text="(esc) для отмены" HorizontalAlignment="Center" Foreground="White"/>
                    </StackPanel>
                </Border>
            </Grid>
            <Grid x:Name="MainContent" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button
                     Grid.Row="0"
                     Command="{Binding StopCommand}"
                     Content="STOP" HorizontalAlignment="Stretch" Width="Auto" VerticalAlignment="Center" Style="{StaticResource StandardButtonStyle}"
                     Margin="12,8" Panel.ZIndex="10" Background="#FF7B3838" Visibility="{Binding Parameters.IsStarted, Converter={StaticResource BoolToVisConverter}}"/>
                <Button
                    Grid.Row="0"
                    Command="{Binding StartCommand}"
                    Content="Start!" HorizontalAlignment="Stretch" Width="Auto" VerticalAlignment="Center" Style="{StaticResource StandardButtonStyle}"
                    Margin="12,8" />
                <Border Grid.Row="1" Background="#FF232428">
                    <Expander x:Name="Expander" Margin="12,4" Header="Advanced Settings" FontSize="15" Foreground="White" Expanded="Expander_Expanded" Collapsed="Expander_Collapsed" IsExpanded="True">
                        <Grid x:Name="ExpandedArea" Margin="48,0">
                            <!-- Объявляем ровно 16 строк, чтобы избежать наложения элементов -->
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <!-- Row 0 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 1 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 2 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 3 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 4 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 5 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 6 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 7 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 8 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 9 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 10 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 11 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 12 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 13 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 14 -->
                                <RowDefinition Height="Auto"/>
                                <!-- Row 15 -->
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" ToolTip="Font size of the translated text window.">
                                <Label Content="Font Size"/>
                                <xctk:IntegerUpDown Value="{Binding Settings.FontSize}" Increment="1" Maximum="200" Minimum="1" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="14"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" ToolTip="Online service used to translate text.">
                                <Label Content="Translator Engine"/>
                                <ComboBox Style="{DynamicResource ComboBoxStyle1}" SelectedItem="{Binding Settings.Translator, Mode=TwoWay}" ItemsSource="{Binding Translators}" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="14">
                                    <ComboBox.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF1E1F22" />
                                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowFrameBrushKey}"/>
                                    </ComboBox.Resources>
                                </ComboBox>
                            </StackPanel>

                            <!-- Выбор целевого языка перевода -->
                            <StackPanel Grid.Row="2" ToolTip="Language to translate text into.">
                                <Label Content="Translate Target Language"/>
                                <ComboBox Style="{DynamicResource ComboBoxStyle1}" SelectedItem="{Binding Settings.TargetLanguage, Mode=TwoWay}" ItemsSource="{Binding TargetLanguages}" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="14">
                                    <ComboBox.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF1E1F22" />
                                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowFrameBrushKey}"/>
                                    </ComboBox.Resources>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Row="3" ToolTip="Google Gemini API Key (API dashboard keys)">
                                <Label Content="Gemini API Key"/>
                                <PasswordBox x:Name="GeminiPasswordBox" PasswordChanged="GeminiPasswordBox_PasswordChanged" Background="#FF1E1F22" Foreground="White" BorderBrush="{x:Null}" Padding="6,4" FontSize="14" SelectionBrush="#FF569DE5"/>
                            </StackPanel>

                            <StackPanel Grid.Row="4" ToolTip="Language model used for OCR text extraction on screen.">
                                <Label Content="OCR Model"/>
                                <ComboBox Style="{DynamicResource ComboBoxStyle1}" SelectedItem="{Binding Settings.OcrModel, Mode=TwoWay}" ItemsSource="{Binding Models}" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="14" IsEnabled="{Binding Parameters.IsStarted, Converter={StaticResource BoolToInvertedBoolConverter}}">
                                    <ComboBox.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF1E1F22" />
                                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowFrameBrushKey}"/>
                                    </ComboBox.Resources>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Row="5" ToolTip="Hotkey for starting/stopping the translator.">
                                <Label Content="Toggle / Translate Hotkey"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding Settings.Key, Mode=OneWay}" FontWeight="Bold" TextAlignment="Center" IsReadOnly="True" Background="#FF1E1F22" Foreground="White" SelectionBrush="{x:Null}" BorderBrush="{x:Null}"/>
                                    <Button Grid.Column="1" Content="Assign" Margin="8,0" Command="{Binding SetKeyCommand}" Style="{StaticResource StandardButtonStyle}" Width="100" Height="26" HorizontalAlignment="Right"/>
                                    <Button Grid.Column="2" Content="Clear" Command="{Binding ClearKeyCommand}" Style="{StaticResource StandardButtonStyle}" Width="100" Height="26" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>

                            <!-- НАСТРОЙКА РУЧНОГО РЕЖИМА -->
                            <StackPanel Grid.Row="6" Margin="0,4" Orientation="Horizontal" ToolTip="If enabled, auto-timer is disabled. Capture and translation will trigger ONLY when you press the hotkey.">
                                <CheckBox IsChecked="{Binding Settings.IsManualModeEnabled}" VerticalAlignment="Center"/>
                                <Label Content="Manual Translation Mode (Translate on Hotkey)" Margin="8,0,0,0" FontWeight="Bold"/>
                            </StackPanel>

                            <StackPanel Grid.Row="7" ToolTip="Delay in seconds between screenshot capture checks." IsEnabled="{Binding Settings.IsManualModeEnabled, Converter={StaticResource BoolToInvertedBoolConverter}}">
                                <Label Content="Capture Interval (sec)"/>
                                <xctk:DecimalUpDown Value="{Binding Settings.Period}" Increment=".1" Maximum="10" Minimum="0.4" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="14" IsEnabled="{Binding Parameters.IsStarted, Converter={StaticResource BoolToInvertedBoolConverter}}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="8" ToolTip="Amount of empty screenshot captures before translation window hides.">
                                <Label Content="Hide Delay (Iterations)"/>
                                <xctk:DecimalUpDown Value="{Binding Settings.HideInterval}" Increment="1" Maximum="10" Minimum="1" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="14" IsEnabled="{Binding Parameters.IsStarted, Converter={StaticResource BoolToInvertedBoolConverter}}"/>
                            </StackPanel>

                            <!-- НАСТРОЙКИ НОВОГО ЦВЕТОВОГО ФИЛЬТРА -->
                            <StackPanel Grid.Row="9" Margin="0,4" Orientation="Horizontal" ToolTip="Enable this filter to only capture and send dialogue boxes of a specific color, preventing AI halluciantions and saving API limits.">
                                <CheckBox IsChecked="{Binding Settings.IsColorFilterEnabled}" VerticalAlignment="Center"/>
                                <Label Content="Use Dialogue Box Color Filter" Margin="8,0,0,0" FontWeight="Bold"/>
                            </StackPanel>

                            <Grid Grid.Row="10" Margin="0,2,0,6" IsEnabled="{Binding Settings.IsColorFilterEnabled}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,6,0" ToolTip="Target Hex Color of dialogue backdrop (e.g. #000000 for black).">
                                    <Label Content="Target HEX Color" FontSize="12"/>
                                    <TextBox Text="{Binding Settings.TargetColorHex, Mode=TwoWay}" Background="#FF1E1F22" Foreground="White" BorderBrush="{x:Null}" Padding="4" FontSize="12"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="6,0,6,0" ToolTip="RGB Color tolerance range (0-100). Higher values allow more color variations.">
                                    <Label Content="Tolerance (0-100)" FontSize="12"/>
                                    <xctk:IntegerUpDown Value="{Binding Settings.ColorTolerance}" Increment="1" Maximum="100" Minimum="0" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="12"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="6,0,0,0" ToolTip="Minimum required percentage of the target color in the capture window (0-100%).">
                                    <Label Content="Min Color Fill %" FontSize="12"/>
                                    <xctk:IntegerUpDown Value="{Binding Settings.MinColorPercentage}" Increment="1" Maximum="100" Minimum="1" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="12"/>
                                </StackPanel>
                            </Grid>

                            <!-- НАСТРОЙКИ ЛОКАЛЬНОГО ПРЕ-ФИЛЬТРА ПЕРЕД GEMINI VISION -->
                            <StackPanel Grid.Row="11" Margin="0,4" Orientation="Horizontal" ToolTip="Use local fast PaddleOCR on your PC to pre-scan. If zero characters are found, we don't call Gemini Vision, saving 100% of limits on empty screens.">
                                <CheckBox IsChecked="{Binding Settings.IsOcrPreFilterEnabled}" VerticalAlignment="Center"/>
                                <Label Content="Use Local OCR Pre-Filter for Gemini Vision" Margin="8,0,0,0" FontWeight="Bold"/>
                            </StackPanel>

                            <StackPanel Grid.Row="12" Margin="20,0,0,4" ToolTip="Select the game language for local PaddleOCR pre-filtering." IsEnabled="{Binding Settings.IsOcrPreFilterEnabled}">
                                <Label Content="Pre-Filter OCR Language" FontSize="12"/>
                                <ComboBox Style="{DynamicResource ComboBoxStyle1}" SelectedItem="{Binding Settings.PreFilterLanguage, Mode=TwoWay}" ItemsSource="{Binding Models}" Background="#FF1E1F22" BorderBrush="{x:Null}" Foreground="White" FontSize="12">
                                    <ComboBox.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF1E1F22" />
                                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowFrameBrushKey}"/>
                                    </ComboBox.Resources>
                                </ComboBox>
                            </StackPanel>

                            <!-- Блок управления областями захвата экрана -->
                            <StackPanel Grid.Row="13" ToolTip="Create and adjust dynamic screen capture locations on your screen.">
                                <Label Content="Screen Capture Areas"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Settings.SelectionWindowPositions.Count, StringFormat='Active areas: {0}'}" Foreground="White" VerticalAlignment="Center" FontSize="14"/>
                                    <Button Grid.Column="1" Content="Add area" Command="{Binding AddSelectionWindowCommand}" Margin="8,0" Style="{StaticResource StandardButtonStyle}" Width="100" Height="26" HorizontalAlignment="Right"/>
                                    <Button Grid.Column="2" Content="Remove area" Command="{Binding RemoveSelectionWindowCommand}" Style="{StaticResource StandardButtonStyle}" Width="100" Height="26" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel Grid.Row="14" Margin="12" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button x:Name="ResetSettingsToDefault" Margin="0,0,8,0" Content="Reset to Defaults" Command="{Binding ResetSettingsToDefaultCommand}" Style="{StaticResource StandardButtonStyle}"/>
                                <Button x:Name="ResetWindowPosition" Content="Reset Windows Position" Command="{Binding ResetWindowsPositionCommand}" Style="{StaticResource StandardButtonStyle}"/>
                            </StackPanel>
                            <TextBlock Grid.Row="15" Margin="0,12,0,8" FontSize="12" HorizontalAlignment="Center">
                                <Hyperlink x:Name="Link" NavigateUri="https://github.com/MaximFrancev/ScreTranPlus" Foreground="#FFAFAFAF">About project</Hyperlink>
                            </TextBlock>
                        </Grid>
                    </Expander>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>